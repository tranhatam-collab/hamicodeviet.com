{
  "name": "hamicodeviet-com",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name hamicodeviet-com --branch main --env-file .env.cloudflare"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^4.16.0"
  }
}
